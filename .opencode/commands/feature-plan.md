---
description: Research a non-trivial feature and create or update its persistent implementation plan.
agent: planner
subtask: true
---

Feature request:

$ARGUMENTS

Research the current repository before designing the change.

Create or update a focused plan under `plans/`.

Use the `feature-planning` skill and all relevant domain skills.

The plan must separate confirmed current behavior from assumptions and include
exact affected files when determinable.

Do not implement application code.
