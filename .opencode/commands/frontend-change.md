---
description: Implement a focused React TypeScript change using existing API and socket conventions.
agent: frontend
subtask: true
---

Implement this frontend change:

$ARGUMENTS

Inspect current code and API contracts first.

Keep transport calls under `web/src/api/` and socket lifecycle centralized.

Do not invent backend behavior.

Run frontend lint/build and relevant tests.
