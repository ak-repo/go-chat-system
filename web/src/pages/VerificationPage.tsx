import { useState, type FormEvent } from 'react';
import { Link } from 'react-router-dom';
import { confirmVerification, requestVerification } from '../api/auth';

export default function VerificationPage() { const [email, setEmail] = useState(''); const [token, setToken] = useState(''); const [message, setMessage] = useState(''); const [error, setError] = useState(''); const [loading, setLoading] = useState(false); const submit = async (event: FormEvent) => { event.preventDefault(); setLoading(true); setError(''); try { const result = await requestVerification(email); if (result.success) setMessage(result.data?.message || 'If the account exists, verification instructions were sent.'); else setError(result.error || 'Could not request verification'); } catch { setError('Could not request verification'); } finally { setLoading(false); } }; const verify = async (event: FormEvent) => { event.preventDefault(); setLoading(true); setError(''); try { const result = await confirmVerification(token); if (result.success) setMessage('Account verified.'); else setError(result.error || 'Invalid or expired verification token'); } catch { setError('Invalid or expired verification token'); } finally { setLoading(false); } }; return <div className="min-h-screen flex items-center justify-center bg-gray-50 p-4"><div className="max-w-md w-full p-8 bg-white rounded-lg shadow-md"><h1 className="text-2xl font-bold mb-6">Verify account</h1>{error && <div className="mb-4 p-3 bg-red-100 text-red-700 rounded">{error}</div>}{message && <div className="mb-4 p-3 bg-green-100 text-green-700 rounded">{message}</div>}<form onSubmit={submit} className="space-y-4"><input type="email" value={email} onChange={(e) => setEmail(e.target.value)} required className="w-full px-3 py-2 border rounded-md" placeholder="Email" /><button disabled={loading} className="w-full py-2 bg-purple-600 text-white rounded-md disabled:opacity-50">Send verification instructions</button></form><form onSubmit={verify} className="space-y-4 border-t mt-8 pt-8"><input value={token} onChange={(e) => setToken(e.target.value)} required className="w-full px-3 py-2 border rounded-md" placeholder="Verification token" /><button disabled={loading} className="w-full py-2 bg-gray-700 text-white rounded-md disabled:opacity-50">Verify account</button></form><Link to="/login" className="block mt-4 text-purple-600">Back to login</Link></div></div>; }
